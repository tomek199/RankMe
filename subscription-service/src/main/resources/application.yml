server:
  port: 9070
spring:
  application:
    name: subscription-service
  config:
    import: optional:configserver:http://localhost:9010
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    function:
      definition: playerCreatedFlux
    stream:
      bindings:
        playerCreatedFlux-in-0:
          destination: RankMe
          group: subscriber-player-created
      rabbit:
        bindings:
          playerCreatedFlux-in-0.consumer.bindingRoutingKey: query-player-created
        default:
          consumer:
            anonymousGroupPrefix: ${spring.application.name}-${server.port}.

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9030/eureka
graphql:
  servlet:
    mapping: /graphql
    enabled: true
    corsEnabled: false
graphiql:
  mapping: /graphiql
  enabled: true
  cdn:
    enabled: false
    version: 0.13.0