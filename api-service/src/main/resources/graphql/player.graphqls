input CreatePlayerCommand {
    leagueId: String!
    name: String!
}

input GetPlayerQuery {
    id: String!
}

type Player {
    id: String!
    name: String!
    deviation: Int!
    rating: Int!
    games(first: Int!, after: String): GameConnection @connection(for: "Game")
    completedGames(first: Int!, after: String): CommpletedGameConnection @connection(for: "CompletedGame")
    scheduledGames(first: Int!, after: String): ScheduledGameConnection @connection(for: "ScheduledGame")
}