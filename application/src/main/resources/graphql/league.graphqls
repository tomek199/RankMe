type League {
    id: String!
    name: String!
    settings: LeagueSettings
    competitors: [Competitor!]!
    games(last: Int!, after: String): GameConnection!
}

type LeagueSettings {
    allowDraws: Boolean
    maxScore: Int
}
